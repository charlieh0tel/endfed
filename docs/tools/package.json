{
  "name": "endfed-tools",
  "private": true,
  "type": "module",
  "description": "Dev-time type checking and tests for docs/random-wire.html.",
  "scripts": {
    "extract": "node extract.mjs ../random-wire.html .check/random-wire.jsx",
    "check": "npm run extract && tsc -p tsconfig.check.json && eslint .check/random-wire.jsx",
    "extract:model": "node extract-model.mjs ../random-wire.html .check/model.mjs",
    "test": "npm run extract && npm run extract:model && node --test model.test.mjs extract.test.mjs",
    "lint": "npm run extract && eslint .check/random-wire.jsx",
    "serve": "node serve.mjs",
    "test:browser": "playwright test"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@playwright/test": "^1.62.1",
    "@types/react": "^18.3.31",
    "@types/react-dom": "^18.3.7",
    "eslint": "^10.8.1",
    "eslint-plugin-react-hooks": "^7.1.1",
    "typescript": "^5.6.0"
  }
}
